apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: worker3-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: worker3-pv

